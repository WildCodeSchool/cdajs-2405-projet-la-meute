npm run check || {
  echo "🚨 Your code is not properly formatted or has linting errors. Please run 'npm run format' and 'npm run lint' in the root directory to fix the issues."
  echo "Once fixed, make sure to run 'git add' again before committing."
  exit 1
}